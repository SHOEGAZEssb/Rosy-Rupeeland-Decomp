#include "tingle/types.h"

/* Recovered type-35 actor virtual category query. */

/* Return the retail category mask; the borrowed actor is not accessed. */
u32 func_020534c4(const void *actor)
{
    (void)actor;
    return 0x100000;
}
