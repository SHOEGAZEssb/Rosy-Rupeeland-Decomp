/*
 * Recovered overlay 94 actor behavior, event, and presentation subsystem.
 *
 * The overlay coordinates lifecycle callbacks, state-driven actor updates,
 * resource and presentation effects, and initialized dispatch tables. Unknown
 * members retain address-based names and offset-oriented descriptions.
 */

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022177e0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022179b0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022179dc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02217a10(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02217bc8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02217be0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02217c34(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02217c54(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02217c74(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02217d20(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02217d64(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02217e94(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02217ec8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_0221800c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02218040(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_0221807c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02218470(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022185a4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022185e4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022186dc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02218720(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022189f8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02218a2c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02218b14(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02218b48(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02218b84(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02218da0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02218ed4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02218f14(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_0221900c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219050(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022191fc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219230(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219234(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219278(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022192a8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022192e0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219344(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219434(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219460(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219494(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219540(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219568(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022196a4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022196e8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022198e8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_0221991c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_022199e0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219a08(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219ad4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219b78(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219bac(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov094_02219bb0(void);

/* Exact read-only parameters and initialized dispatch data used by the overlay; individual semantic meanings remain unconfirmed. */
extern const unsigned char data_ov094_02219bd8[];
extern unsigned char data_ov094_02219da0[];

