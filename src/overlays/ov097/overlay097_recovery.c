/*
 * Recovered overlay 97 actor lifecycle, behavior, and presentation subsystem.
 *
 * The overlay coordinates state-driven engine callbacks, interaction and visual
 * effects, resource ownership, and initialized dispatch data. Unknown members
 * retain address-based names and offset-oriented descriptions.
 */

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022177e0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217804(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217934(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221794c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217974(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022179a4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217ac0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217ac4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217afc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217b14(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217b4c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217cec(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217cf8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217d44(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217fd8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02217fec(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02218034(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022180b8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022180bc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221818c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221819c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022181a0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022188f8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02218940(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02218b1c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02218b3c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02218b40(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02218d04(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02218d3c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022191b8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219228(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219458(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022194e8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221951c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022195d8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022195e0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022195ec(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022195f4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219718(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221972c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219748(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219760(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221978c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022197e0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_022198e8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219970(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219aa8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219b50(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219b58(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219b60(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219b70(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219b7c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219b90(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219b94(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219b98(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219ba0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219ba8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219bb0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219bc8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219be0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219be8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219c30(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219c44(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219c64(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219c7c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219c94(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219cac(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219cb4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219cbc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219cc0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219cc4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219ce4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219cec(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219cf4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219cf8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219d08(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219d14(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219d1c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219d20(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219e4c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219e80(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_02219ebc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a004(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a05c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a07c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a1c8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a3b8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a420(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a43c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a450(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a458(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a45c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a460(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a470(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a478(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a480(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a488(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a490(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov097_0221a494(void);

/* Exact read-only parameters and initialized dispatch data used by the overlay; individual semantic meanings remain unconfirmed. */
extern const unsigned char data_ov097_0221a49c[];
extern unsigned char data_ov097_0221a4c0[];

