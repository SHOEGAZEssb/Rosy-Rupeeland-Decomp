/*
 * Recovered overlay 75 actor-derived behavior and interaction subsystem.
 *
 * The overlay implements several related actor constructors, lifecycle paths,
 * interaction/state callbacks, transform helpers, and scene-facing dispatchers.
 * Unconfirmed members and small virtual helpers retain address-based names and
 * offset-based descriptions.
 */

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_0203db80; exact higher-level semantics remain unconfirmed. */
void func_ov075_02212ae0(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_0203db80; exact higher-level semantics remain unconfirmed. */
void func_ov075_02212bb8(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02007f0c, func_020435f4, func_ov075_02212d80 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_02212c90(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02212d80(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_0203e494; exact higher-level semantics remain unconfirmed. */
void func_ov075_02212dc8(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_0203e494; exact higher-level semantics remain unconfirmed. */
void func_ov075_02212ddc(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02212df8(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02212e04(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes Actor_SetPosition, func_ov075_02212d80, func_ov075_02212f10; exact higher-level semantics remain unconfirmed. */
void func_ov075_02212e4c(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02212f10(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020050a4, Actor_GetCachedTerrainHeight; exact higher-level semantics remain unconfirmed. */
void func_ov075_02212f20(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020349b8, func_020405c8; exact higher-level semantics remain unconfirmed. */
void func_ov075_02212f98(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020050a4, func_02031720, Actor_GetCachedTerrainHeight and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_02212fd8(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020ae024; exact higher-level semantics remain unconfirmed. */
void func_ov075_022132e8(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02213368(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020050a4, Actor_GetCachedTerrainHeight, func_02072b68; exact higher-level semantics remain unconfirmed. */
void func_ov075_022133a4(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_0221343c(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02005030, func_02005058, func_02050078; exact higher-level semantics remain unconfirmed. */
void func_ov075_02213480(void);

/* Empty or constant ABI helper: accepts its implicit arguments, changes no observable state, returns its encoded ABI result, and performs no SDK or hardware work. */
void func_ov075_02213528(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02003e20, func_02004fe0, func_020435f4 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_0221352c(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02005058, func_0203e494; exact higher-level semantics remain unconfirmed. */
void func_ov075_022135c4(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02005058, func_0203e494; exact higher-level semantics remain unconfirmed. */
void func_ov075_022135e4(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_0201f864, Actor_SetPosition, func_020593dc and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_0221360c(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020050a4, func_02031720, Actor_GetCachedTerrainHeight and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_022137a0(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02213c04(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02213ca4(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_ov075_02212fd8; exact higher-level semantics remain unconfirmed. */
void func_ov075_02213cc0(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02005030, func_02005058, func_020050a4 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_02213cfc(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020064b8; exact higher-level semantics remain unconfirmed. */
void func_ov075_02213ec4(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02004fe0, func_0200500c, func_02005058 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_02213f08(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_0221424c(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020593dc, func_02072b68, func_ov075_02213cfc; exact higher-level semantics remain unconfirmed. */
void func_ov075_02214260(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022144b8(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022144c0(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022144d0(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022144e4(void);

/* Empty or constant ABI helper: accepts its implicit arguments, changes no observable state, returns its encoded ABI result, and performs no SDK or hardware work. */
void func_ov075_022144ec(void);

/* Empty or constant ABI helper: accepts its implicit arguments, changes no observable state, returns its encoded ABI result, and performs no SDK or hardware work. */
void func_ov075_022144f0(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022144f4(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022144fc(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02214504(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_0221450c(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02214524(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_0221453c(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02214544(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02214554(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02214568(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02214588(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022145a0(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022145b8(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022145d0(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022145d8(void);

/* Empty or constant ABI helper: accepts its implicit arguments, changes no observable state, returns its encoded ABI result, and performs no SDK or hardware work. */
void func_ov075_022145e0(void);

/* Empty or constant ABI helper: accepts its implicit arguments, changes no observable state, returns its encoded ABI result, and performs no SDK or hardware work. */
void func_ov075_022145e4(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022145e8(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02214608(void);

/* Empty or constant ABI helper: accepts its implicit arguments, changes no observable state, returns its encoded ABI result, and performs no SDK or hardware work. */
void func_ov075_02214610(void);

/* Empty or constant ABI helper: accepts its implicit arguments, changes no observable state, returns its encoded ABI result, and performs no SDK or hardware work. */
void func_ov075_02214614(void);

/* Empty or constant ABI helper: accepts its implicit arguments, changes no observable state, returns its encoded ABI result, and performs no SDK or hardware work. */
void func_ov075_02214618(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02004fe0, func_020050a4, func_0203b514; exact higher-level semantics remain unconfirmed. */
void func_ov075_0221461c(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02005058, func_0203b61c; exact higher-level semantics remain unconfirmed. */
void func_ov075_022146a4(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02005058, func_0203b61c; exact higher-level semantics remain unconfirmed. */
void func_ov075_022146c4(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes ActorCollection_GetSpriteOwner, Actor_GetCollection, func_02071ee0 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_022146ec(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02005058, func_020050a4, Actor_QueryTerrainCell and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_02214770(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02004fe0, func_020adfbc; exact higher-level semantics remain unconfirmed. */
void func_ov075_02214898(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_0200222c, func_020022dc, func_02005030 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_022148d0(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02214d88(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes ActorCollection_GetSpriteOwner, Actor_GetCollection, func_02072b68 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_02214e28(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020050a4, Actor_GetCachedTerrainHeight; exact higher-level semantics remain unconfirmed. */
void func_ov075_02214f18(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02214ff8(void);

/* Empty or constant ABI helper: accepts its implicit arguments, changes no observable state, returns its encoded ABI result, and performs no SDK or hardware work. */
void func_ov075_02215000(void);

/* Empty or constant ABI helper: accepts its implicit arguments, changes no observable state, returns its encoded ABI result, and performs no SDK or hardware work. */
void func_ov075_02215004(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02215008(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02215018(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02215020(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02215028(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02215030(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02215038(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02215044(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_0204362c, func_02043cd0, func_ov075_02215044 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_02215068(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02043d4c; exact higher-level semantics remain unconfirmed. */
void func_ov075_022150c4(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02043d4c; exact higher-level semantics remain unconfirmed. */
void func_ov075_022150d8(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_0204368c; exact higher-level semantics remain unconfirmed. */
void func_ov075_022150f4(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_0203f2ec; exact higher-level semantics remain unconfirmed. */
void func_ov075_02215110(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_0221513c(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02215188(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_ov075_02215910; exact higher-level semantics remain unconfirmed. */
void func_ov075_022151bc(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020425d4, func_ov075_02215910; exact higher-level semantics remain unconfirmed. */
void func_ov075_022151f4(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02042810, func_020593dc, func_ov075_022152ac; exact higher-level semantics remain unconfirmed. */
void func_ov075_02215218(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022152ac(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02043d60, func_ov075_02215044, func_ov075_022152ac and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_022152f4(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_022154e8(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02042408, func_ov075_02215910; exact higher-level semantics remain unconfirmed. */
void func_ov075_02215574(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_0221558c(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes Actor_RestoreSavedFlags; exact higher-level semantics remain unconfirmed. */
void func_ov075_022157c8(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_0221582c(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020430ec; exact higher-level semantics remain unconfirmed. */
void func_ov075_022158a0(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_ov075_02215930; exact higher-level semantics remain unconfirmed. */
void func_ov075_022158bc(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_ov075_02215930; exact higher-level semantics remain unconfirmed. */
void func_ov075_02215910(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02029b70; exact higher-level semantics remain unconfirmed. */
void func_ov075_02215930(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02004fe0, func_02043de4; exact higher-level semantics remain unconfirmed. */
void func_ov075_022159c4(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02005058, func_02043e68; exact higher-level semantics remain unconfirmed. */
void func_ov075_02215a28(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02005058, func_02043e68; exact higher-level semantics remain unconfirmed. */
void func_ov075_02215a48(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes ActorCollection_GetSpriteOwner, Actor_GetCollection, func_02072b68 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_02215a70(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02042810, func_020593dc, func_ov075_022152ac; exact higher-level semantics remain unconfirmed. */
void func_ov075_02215c90(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020050a4, func_02043e7c, func_02044088 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_02215cf8(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_0221609c(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02044088, func_ov075_02212f10; exact higher-level semantics remain unconfirmed. */
void func_ov075_022160a4(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02044098; exact higher-level semantics remain unconfirmed. */
void func_ov075_02216158(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_020050a4, func_02044058, func_ov075_0221625c; exact higher-level semantics remain unconfirmed. */
void func_ov075_02216174(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_0221625c(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes Actor_GetCachedTerrainHeight, func_0204aff4, func_0204cfa4 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_0221627c(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_0200500c, func_02005058, func_02009d78 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_0221647c(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02004fe0, func_020adc90; exact higher-level semantics remain unconfirmed. */
void func_ov075_022168a8(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02004fe0, func_020adff0; exact higher-level semantics remain unconfirmed. */
void func_ov075_022168f0(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes func_02004fe0, func_02005030, func_02005058 and other shared helpers; exact higher-level semantics remain unconfirmed. */
void func_ov075_02216928(void);

/* Processes the supplied ABI arguments and offset-based actor state, returns its encoded result, and invokes Actor_RestoreSavedFlags; exact higher-level semantics remain unconfirmed. */
void func_ov075_02216b5c(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02216bb8(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02216c20(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02216c68(void);

/* Processes the supplied ABI arguments using only local offset-based fields; observable mutations and the return value are preserved by the matching implementation, with no direct SDK call. */
void func_ov075_02216c8c(void);
