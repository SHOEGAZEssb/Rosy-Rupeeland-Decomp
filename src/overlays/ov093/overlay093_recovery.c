/*
 * Recovered overlay 93 character-event and boss-stage behavior subsystem.
 *
 * The overlay coordinates actor lifecycle and task callbacks, character-event
 * dispatch, resource/state updates, and a callback table used by the surrounding
 * engine. Unconfirmed fields retain address-based names and offset descriptions.
 */

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022177e0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022177f0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02217908(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02217ad8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02217b90(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02217c50(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218500(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218514(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022186bc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218708(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_0221871c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218724(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_0221872c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218734(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218744(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218750(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218764(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_0221876c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218770(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218774(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218778(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218788(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218790(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_02218798(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022187a0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022187a8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022187ac(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022187b0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022187b4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022187c0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022187c8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022187cc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022187d0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022187d8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, and the encoded return value while higher-level semantics remain unconfirmed. */
void func_ov093_022187e0(void);

/* Exact read-only parameters and initialized callback/name tables referenced by this overlay; individual semantic meanings remain unconfirmed. */
extern const unsigned char data_ov093_022187f0[];
extern unsigned char data_ov093_02218860[];

