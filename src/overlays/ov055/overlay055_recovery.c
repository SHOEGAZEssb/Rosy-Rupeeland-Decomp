#include "tingle/types.h"\n\n/*\n * Overlay 55 paired-screen animated-object scene. It owns an effect collection,\n * loads main/sub graphics and palettes, and drives randomized scene callbacks.\n * Exact assembly preserves resource, RNG, indirect-callback, and MMIO ordering.\n */\n\n/*\n * Initialize and place one scene-owned animated object using the recovered resource, transform, and render helpers. Object and SDK graphics state change; nothing is returned.\n */\nextern "C" void *func_ov055_0220e400(void *object, ...);\n\n/*\n * Randomize one object record, apply the recovered fixed-point distribution, and submit it through the object helper. Object/RNG/SDK state changes; nothing is returned.\n */\nextern "C" void *func_ov055_0220e518(void *object, ...);\n\n/*\n * Construct the scene-owned effect collection and its embedded array, allocate a child object, and initialize all render/animation fields. Return the collection; heap and SDK state change.\n */\nextern "C" void *func_ov055_0220e574(void *object, ...);\n\n/*\n * Free the optional child and destroy the embedded object array. Collection/heap/SDK state changes; nothing is returned.\n */\nextern "C" void *func_ov055_0220e7cc(void *object, ...);\n\n/*\n * Randomize and advance one collection element, update its object state, and invoke func_ov055_0220e518. Element/RNG/SDK state changes; nothing is returned.\n */\nextern "C" void *func_ov055_0220e804(void *object, ...);\n\n/*\n * Apply the recovered display-control bitfield. Display MMIO changes; nothing is returned and the argument semantics remain inferred.\n */\nextern "C" void *func_ov055_0220e860(void *object, ...);\n\n/*\n * Store the recovered coordinate/scroll fields in caller-owned scene state. Scene memory changes; nothing is returned and no SDK/MMIO occurs.\n */\nextern "C" void *func_ov055_0220e8a0(void *object, ...);\n\n/*\n * Construct caller-owned scene state, initialize its base/resources, allocate and construct the effect collection, and install a callback. Return the scene; heap/SDK state changes.\n */\nextern "C" void *func_ov055_0220e8b8(void *object, ...);\n\n/*\n * Destroy the scene without freeing its storage, releasing graphics/effects and resetting display fields. Return the scene; graphics/SDK state change.\n */\nextern "C" void *func_ov055_0220e96c(void *object, ...);\n\n/*\n * Destroy scene-owned collection/resources without freeing scene storage and return it. Heap, transition, graphics, and SDK state change.\n */\nextern "C" void *func_ov055_0220ea68(void *object, ...);\n\n/*\n * Perform full scene teardown, free scene storage, and return its former identity. Heap/SDK state changes; the result must not be dereferenced.\n */\nextern "C" void *func_ov055_0220eae4(void *object, ...);\n\n/*\n * Advance the scene state machine, update animation/effect collections, and drive transitions. Scene, graphics, audio, and SDK state change; return the recovered status.\n */\nextern "C" void *func_ov055_0220eb68(void *object, ...);\n\n/*\n * Load and configure paired main/sub graphics resources, BG state, palettes, and transforms. VRAM, palette, display MMIO, and SDK state change; nothing is returned.\n */\nextern "C" void *func_ov055_0220ebec(void *object, ...);\n\n/*\n * Apply the recovered BG/display bitfield. Display MMIO changes; nothing is returned and the bit meanings remain offset-derived.\n */\nextern "C" void *func_ov055_0220eee4(void *object, ...);\n\n/*\n * Copy the recovered scene transform/coordinate values into destination state. Destination memory changes; nothing is returned and no SDK/MMIO occurs.\n */\nextern "C" void *func_ov055_0220eefc(void *object, ...);\n\n/*\n * Allocate, construct, and register the scene, returning its allocation. Heap, scene-manager, and graphics state change.\n */\nextern "C" void *func_ov055_0220ef6c(void *object, ...);\n\n/*\n * Update the scene coordinate callback and submit both recovered resources. Scene and SDK graphics state change; nothing is returned.\n */\nextern "C" void *func_ov055_0220efe0(void *object, ...);\n\n/*\n * Intentional no-op callback; input and all observable state remain unchanged.\n */\nextern "C" void *func_ov055_0220f010(void *object, ...);\n\n/*\n * Intentional no-op callback; input and all observable state remain unchanged.\n */\nextern "C" void *func_ov055_0220f014(void *object, ...);\n\n/*\n * Intentional no-op callback; input and all observable state remain unchanged.\n */\nextern "C" void *func_ov055_0220f018(void *object, ...);\n\n
