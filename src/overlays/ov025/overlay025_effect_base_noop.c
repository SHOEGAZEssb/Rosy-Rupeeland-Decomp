#include "tingle/types.h"

/* Overlay 25 empty base lifecycle hook used by effect-controller teardown. */

/* Leaves `object` and all observable state unchanged; returns no value. */
extern "C" void func_ov025_021fdec8(void *object)
{
    (void)object;
}
