#include "tingle/types.h"\n\n/*\n * Overlay 58 randomized graphics/effect scene. It owns arrays, tasks, resources,\n * palettes, and a GameWork-gated state machine. Exact assembly preserves the\n * compiler-sensitive cleanup, fixed-point, resource, and transition ordering.\n */\n\n/*\n * Initialize/update one randomized scene effect using animation, render, and GameWork-dependent state. Effect, RNG/graphics SDK state may change; return behavior is preserved.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220e400(void *scene, ...);\n\n/*\n * Randomize one effect record using the RNG and fixed-point helpers. Record/RNG/SDK state changes; nothing is returned.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220e598(void *scene, ...);\n\n/*\n * Clear the recovered scene buffer and initialize its associated helper. Caller-owned memory and SDK state change; nothing is returned.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220e5fc(void *scene, ...);\n\n/*\n * Read or store the first offset-derived scene control value. Only caller-owned memory may change; exact return behavior is preserved and no SDK/MMIO occurs.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220e63c(void *scene, ...);\n\n/*\n * Read or store the second offset-derived scene control value. Only caller-owned memory may change; exact return behavior is preserved and no SDK/MMIO occurs.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220e664(void *scene, ...);\n\n/*\n * Read or store the third offset-derived scene control value. Only caller-owned memory may change; exact return behavior is preserved and no SDK/MMIO occurs.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220e68c(void *scene, ...);\n\n/*\n * Transform recovered scene/object coordinates and state using fixed-point arithmetic. Caller-owned memory changes; no SDK call occurs and field roles remain inferred.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220e6b0(void *scene, ...);\n\n/*\n * Construct the scene, embedded arrays, graphics/task resources, and allocated effect object, then clear its GameWork flag. Return the scene; heap/GameWork/SDK state changes.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220e840(void *scene, ...);\n\n/*\n * Destroy scene resources without freeing scene storage and return it. Heap, graphics, transition, array, and SDK state change.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220ea68(void *scene, ...);\n\n/*\n * Perform full scene teardown, including owned allocations, and return the former scene identity. Heap/SDK state changes; the result must not be dereferenced.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220eb50(void *scene, ...);\n\n/*\n * Compute the recovered offset-derived scene predicate or index. No SDK/MMIO occurs; caller memory and exact return value follow the matching body.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220ec40(void *scene, ...);\n\n/*\n * Prepare graphics state and delegate resource setup through func_ov058_0220f308. Scene/VRAM/SDK state changes; nothing is returned.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220eca4(void *scene, ...);\n\n/*\n * Advance scene-owned task, object, and animation resources through the recovered update sequence. Scene and SDK state change; return behavior is preserved.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220ed64(void *scene, ...);\n\n/*\n * Set the recovered GameWork flag and apply resource state through func_ov058_0220f228. GameWork/graphics state changes; nothing is returned.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220eef0(void *scene, ...);\n\n/*\n * Drive the main scene state machine, flags, transforms, RNG effects, transitions, and completion checks. Scene/GameWork/RNG/SDK state changes; return the recovered status.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220efa4(void *scene, ...);\n\n/*\n * Load the recovered graphics resource set and apply its rendering metadata. VRAM/graphics SDK state changes; nothing is returned.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220f11c(void *scene, ...);\n\n/*\n * Apply the recovered paired graphics resources through SDK resource helpers. Graphics/VRAM state changes; nothing is returned.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220f228(void *scene, ...);\n\n/*\n * Initialize, load, apply, and destroy temporary graphics resources, configure palettes/BG data, and publish scene resources. VRAM/palette/display SDK state changes.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220f308(void *scene, ...);\n\n/*\n * Allocate, construct, and register the scene, returning its allocation. Heap and scene-manager/graphics state change.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220f484(void *scene, ...);\n\n/*\n * Submit the scene's recovered graphics resources through func_02074110. SDK graphics state changes; nothing is returned.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220f4e8(void *scene, ...);\n\n/*\n * Intentional no-op callback; input and all observable state remain unchanged.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220f51c(void *scene, ...);\n\n/*\n * Intentional no-op callback; input and all observable state remain unchanged.\n * Inputs retain their recovered ABI/offset interpretation; direct hardware effects\n * are limited to the cited graphics helpers or explicit matching-body accesses.\n */\nextern "C" void *func_ov058_0220f520(void *scene, ...);\n\n
