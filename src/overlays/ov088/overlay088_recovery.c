/*
 * Recovered overlay 88 actor-derived lifecycle, behavior, and interaction subsystem.
 *
 * The overlay coordinates actor construction and teardown, extensive state-driven
 * updates, interaction and presentation effects, resource ownership, transforms,
 * and compact virtual or offset-field helpers. Unconfirmed members retain
 * address-based names and offset-based descriptions.
 */

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_022177e0(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_022179e0(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_022179f4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02217a18(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02217a3c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02217c3c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02217c54(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02217d48(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02217e44(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02217f38(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02218130(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02218280(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02218290(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02218328(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02219154(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02219168(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_022191d4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221963c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_022198ac(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02219960(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02219a2c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02219c00(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02219c0c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02219d64(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02219d9c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_02219ff4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221a298(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221a58c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221a7c8(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221a8c4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221a930(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221a98c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221a9c0(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221aaac(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221ad6c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221ad84(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221ad9c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221add8(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221af2c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b098(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b218(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b278(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b2c4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b2e0(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b380(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b394(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b3b4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b3c8(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b3e4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b428(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b508(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b528(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b53c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b558(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b5b0(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b5b8(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b5c0(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b5d0(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b5dc(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b5f0(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b5f8(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b5fc(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b600(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b604(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b614(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b61c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b624(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b62c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b634(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b638(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b63c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b648(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov088_0221b654(void);


