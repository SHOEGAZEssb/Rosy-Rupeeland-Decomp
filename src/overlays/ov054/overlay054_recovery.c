#include "tingle/types.h"\n\n/*\n * Overlay 54 paired field-effect/scene lifecycles. The recovered code owns\n * animation and graphics resources, drives GameWork-gated transitions, and\n * configures display/palette state. Exact assembly preserves SDK call order.\n */\n\n/*\n * Construct the first embedded effect object in caller-owned storage, initialize animation/render resources, and return the object. Object and SDK graphics state change; no direct MMIO occurs.\n */\nextern "C" void *func_ov054_0220e400(void *object, ...);\n\n/*\n * Destroy the first effect object without freeing its storage and return it. Owned animation/render and SDK state change; no direct MMIO occurs.\n */\nextern "C" void *func_ov054_0220e4a0(void *object, ...);\n\n/*\n * Destroy and free the first effect object, returning its former address identity. Heap and SDK state change; the result must not be dereferenced.\n */\nextern "C" void *func_ov054_0220e4c8(void *object, ...);\n\n/*\n * Forward the offset-derived vector fields to func_ov054_0220e52c. Object transform state changes; nothing is returned and no direct MMIO occurs.\n */\nextern "C" void *func_ov054_0220e4f8(void *object, ...);\n\n/*\n * Copy the recovered coordinate components between offset-derived records. Destination memory changes; nothing is returned and no SDK or MMIO occurs.\n */\nextern "C" void *func_ov054_0220e52c(void *object, ...);\n\n/*\n * Advance the first effect animation and transform using collision/angle and render helpers. Object and SDK graphics state change; the recovered status is returned.\n */\nextern "C" void *func_ov054_0220e560(void *object, ...);\n\n/*\n * Construct the derived first effect through func_ov054_0220e400 and install derived state. Return the object; SDK/object state changes.\n */\nextern "C" void *func_ov054_0220e68c(void *object, ...);\n\n/*\n * Destroy the derived first effect without freeing storage and return it. Transition, animation, render, and SDK state change.\n */\nextern "C" void *func_ov054_0220e6b4(void *object, ...);\n\n/*\n * Destroy and free the derived first effect, returning its former identity. Heap/SDK state changes; the result is invalid to dereference.\n */\nextern "C" void *func_ov054_0220e704(void *object, ...);\n\n/*\n * Advance the first scene state machine, testing GameWork, loading graphics, updating effects, and driving transitions. Scene/graphics/GameWork state changes; return the recovered status.\n */\nextern "C" void *func_ov054_0220e75c(void *object, ...);\n\n/*\n * Store the recovered transform fields for the first scene. Scene memory changes; nothing is returned and no SDK or MMIO occurs.\n */\nextern "C" void *func_ov054_0220e96c(void *object, ...);\n\n/*\n * Store the alternate recovered transform fields for the first scene. Scene memory changes; nothing is returned and no SDK or MMIO occurs.\n */\nextern "C" void *func_ov054_0220e994(void *object, ...);\n\n/*\n * Allocate, construct, and register the first scene, returning its allocation. Heap and scene-manager state change; no direct MMIO occurs.\n */\nextern "C" void *func_ov054_0220e9bc(void *object, ...);\n\n/*\n * Return the recovered readiness predicate from offset-derived scene fields. No state changes and no SDK or MMIO occurs.\n */\nextern "C" void *func_ov054_0220ea18(void *object, ...);\n\n/*\n * Call the recovered first-scene update path and return zero for the callback ABI. Scene/SDK state may change through the callee.\n */\nextern "C" void *func_ov054_0220ea24(void *object, ...);\n\n/*\n * Initialize/load/apply the first scene graphics resource set and palette, then destroy temporaries. VRAM, palette, graphics SDK, and display state change.\n */\nextern "C" void *func_ov054_0220ea34(void *object, ...);\n\n/*\n * Apply the recovered display-control bitfield. Display MMIO changes; nothing is returned and the argument semantics remain inferred.\n */\nextern "C" void *func_ov054_0220eb1c(void *object, ...);\n\n/*\n * Update the first scene transition from GameWork and offset-derived state. Scene, GameWork, and SDK transition state change; return the recovered status.\n */\nextern "C" void *func_ov054_0220eb50(void *object, ...);\n\n/*\n * Run first-scene graphics setup and return zero for its callback ABI. Graphics/VRAM state changes through the callee.\n */\nextern "C" void *func_ov054_0220ec14(void *object, ...);\n\n/*\n * Apply the recovered graphics transform/position through func_020ae024. SDK graphics state changes; nothing is returned.\n */\nextern "C" void *func_ov054_0220ec4c(void *object, ...);\n\n/*\n * Construct the second scene/effect object, initialize embedded animation records, and seed randomized state. Return the object; RNG and SDK state change.\n */\nextern "C" void *func_ov054_0220ec6c(void *object, ...);\n\n/*\n * Destroy the second object without freeing storage and return it. Owned animation/render and SDK state change.\n */\nextern "C" void *func_ov054_0220ed0c(void *object, ...);\n\n/*\n * Destroy and free the second object, returning its former identity. Heap/SDK state changes; the result is invalid to dereference.\n */\nextern "C" void *func_ov054_0220ed54(void *object, ...);\n\n/*\n * Advance the second object state machine, handling flags, transitions, animations, and render records. Object/GameWork/audio/SDK state changes; return the recovered status.\n */\nextern "C" void *func_ov054_0220eda4(void *object, ...);\n\n/*\n * Return the recovered constant callback status without changing state or accessing SDK/MMIO.\n */\nextern "C" void *func_ov054_0220ef78(void *object, ...);\n\n/*\n * Allocate, construct, and register the second object, returning its allocation. Heap and scene-manager state change.\n */\nextern "C" void *func_ov054_0220ef80(void *object, ...);\n\n/*\n * Intentional no-op callback; input and all observable state remain unchanged.\n */\nextern "C" void *func_ov054_0220efe0(void *object, ...);\n\n/*\n * Intentional no-op callback; input and all observable state remain unchanged.\n */\nextern "C" void *func_ov054_0220efe4(void *object, ...);\n\n/*\n * Intentional no-op callback; input and all observable state remain unchanged.\n */\nextern "C" void *func_ov054_0220efe8(void *object, ...);\n\n/*\n * Intentional no-op callback; input and all observable state remain unchanged.\n */\nextern "C" void *func_ov054_0220efec(void *object, ...);\n\n/*\n * Intentional no-op callback; input and all observable state remain unchanged.\n */\nextern "C" void *func_ov054_0220eff0(void *object, ...);\n\n
