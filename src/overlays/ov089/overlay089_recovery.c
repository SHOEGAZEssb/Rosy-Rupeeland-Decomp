/*
 * Recovered overlay 89 actor-derived lifecycle, behavior, and static setup subsystem.
 *
 * The overlay coordinates actor lifecycle paths, state-driven interaction and
 * presentation updates, resource ownership, transforms, compact virtual helpers,
 * and a static initializer for overlay-owned global records. Unconfirmed members
 * retain address-based names and offset-based descriptions.
 */

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022177e0(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022177e4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02217890(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022178ac(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022178d4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02217904(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02217960(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022179dc(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02217b04(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02217b94(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02217c14(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02217c60(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022186b4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02218720(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02218954(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02218a5c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02218c44(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02218c9c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02218cb4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02218e6c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02218e80(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02218f28(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02219214(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02219224(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02219540(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02219548(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02219550(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02219560(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_0221956c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02219580(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02219588(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_0221958c(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02219590(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_02219594(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022195a4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022195ac(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022195b4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022195bc(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022195c4(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022195c8(void);

/* Processes the supplied ABI arguments and offset-based object state; observable mutations, calls, and the encoded return value are preserved by the matching implementation, while higher-level semantics remain unconfirmed. */
void func_ov089_022195d4(void);

/* Initializes overlay-owned global records during module startup; it takes no confirmed external input, mutates the referenced globals, returns no meaningful value, and invokes shared initialization helpers. */
void __sinit_ov089_022195e8(void);

