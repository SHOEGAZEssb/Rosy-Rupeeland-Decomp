/*
 * Recovered overlay 96 actor lifecycle and state-driven behavior subsystem.
 *
 * The overlay coordinates engine callbacks, object-state transitions, resource
 * ownership, and initialized dispatch data. Unknown members retain address-based
 * names and offset-oriented descriptions.
 */

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022177e0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02217868(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_0221787c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02217898(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022178c0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02217e88(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02217ea4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02217eb0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02217ec8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02217ef8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02217f4c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02217f6c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218108(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_0221811c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218178(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218180(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218190(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022181a4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022181ac(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022181b0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022181b4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022181bc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022181c4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022181dc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022181f4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022181fc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_0221820c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218254(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218268(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218288(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022182a0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022182b8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022182d0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022182d8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022182e0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022182e4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022182e8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218308(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218310(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_0221835c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218370(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_0221838c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218590(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_022185d8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_0221865c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218668(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_0221880c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_0221881c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_0221883c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218958(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218aa4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov096_02218adc(void);

/* Exact initialized dispatch data used by the overlay; individual semantic meanings remain unconfirmed. */
extern unsigned char data_ov096_02218b00[];

