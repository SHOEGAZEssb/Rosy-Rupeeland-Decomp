/*
 * Recovered overlay 101 compact task and presentation subsystem.
 *
 * The overlay coordinates a small lifecycle/update path and its parameter and
 * dispatch data. Unknown members retain address-based names and offset-oriented
 * descriptions.
 */

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov101_0221d3e0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov101_0221d408(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov101_0221d438(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov101_0221d470(void);

/* Exact read-only parameters and initialized dispatch data used by the overlay; individual semantic meanings remain unconfirmed. */
extern const unsigned char data_ov101_0221d5e8[];
extern unsigned char data_ov101_0221d620[];

