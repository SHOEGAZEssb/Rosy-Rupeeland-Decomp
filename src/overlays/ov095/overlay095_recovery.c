/*
 * Recovered overlay 95 actor lifecycle, state-machine, and presentation subsystem.
 *
 * The overlay coordinates a large set of behavior callbacks, resource ownership,
 * animation and interaction effects, and initialized dispatch data. Unconfirmed
 * members retain address-based names and offset-oriented descriptions.
 */

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_022177e0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02217980(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02217998(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_022179e4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02217a38(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02217aac(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02217b24(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02217c40(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02217d58(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02217d90(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02217da8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02217df4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_022180dc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_022180f0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02218124(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02218130(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02218404(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02218414(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221844c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02218450(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_022184b4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_02218534(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221858c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221a208(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221a250(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221a294(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221a2b4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221a3f8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221a418(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221a878(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221abc8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221ac20(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221ac6c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221ae14(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221ae5c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221ae9c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221aedc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221af0c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b060(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b0a4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b0ac(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b0bc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b0c8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b0dc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b0e0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b0e4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b0ec(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b0f4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b13c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b150(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b170(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b188(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b1a0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b1b8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b1c0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b1c8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b1cc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b1ec(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b1f4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b1f8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b208(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b214(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b21c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b224(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b22c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b234(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b238(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b23c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b2a0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b2c0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b2e8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b304(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b38c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b6d8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b720(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b798(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221b7c8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221ba38(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221bbcc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221bbf0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221bd68(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221bda4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221bed0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221bf8c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c054(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c05c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c064(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c06c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c084(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c09c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c0ac(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c0b0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c0e4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c130(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c184(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c1d0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c338(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c344(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c3a0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c3ac(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c4a4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c690(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c6a4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c6c4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c6c8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c6cc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c6dc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c6e4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c6ec(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c6f4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c6fc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c700(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c71c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c724(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c74c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c760(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c77c(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c7d4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c8d0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c8dc(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221c8e8(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221ca14(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221cae0(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221caf4(void);

/* Processes ABI arguments and offset-based object state; preserves calls, observable mutations, return values, and engine effects while higher-level semantics remain unconfirmed. */
void func_ov095_0221cb14(void);

/* Exact read-only parameters and initialized dispatch data used by the overlay; individual semantic meanings remain unconfirmed. */
extern const unsigned char data_ov095_0221cb34[];
extern unsigned char data_ov095_0221cba0[];

