#include "tingle/types.h"\n\n/*\n * Overlay 59 interactive touch/menu scene. The recovered subsystem owns graphics\n * resources, touch regions, embedded arrays, transitions, sound, and a large\n * scene state machine. Address names remain where field roles are unconfirmed.\n */\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x0220fd20.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_0220fd20(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x0220fd64.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_0220fd64(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x0220fd80.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_0220fd80(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022100c0.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022100c0(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022100dc.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022100dc(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022100f8.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022100f8(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210114.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210114(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210178.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210178(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022101b0.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022101b0(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x0221026c.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_0221026c(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210284.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210284(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022102ac.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022102ac(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210308.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210308(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022104f0.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022104f0(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210508.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210508(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210554.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210554(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210578.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210578(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x0221058c.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_0221058c(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022105a8.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022105a8(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210600.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210600(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210620.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210620(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x0221063c.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_0221063c(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x0221065c.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_0221065c(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x0221067c.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_0221067c(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022106f0.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022106f0(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022106fc.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022106fc(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210950.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210950(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210968.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210968(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022109bc.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022109bc(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022109f0.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022109f0(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210a2c.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210a2c(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210ab0.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210ab0(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210b24.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210b24(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210ba4.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210ba4(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210bc4.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210bc4(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210c24.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210c24(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210c60.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210c60(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210cb8.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210cb8(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210cf8.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210cf8(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210d90.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210d90(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210db0.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210db0(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210dfc.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210dfc(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210f34.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210f34(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02210fec.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02210fec(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02211134.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02211134(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022111d4.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022111d4(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x0221127c.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_0221127c(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x0221129c.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_0221129c(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02211330.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02211330(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02211854.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02211854(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02211870.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02211870(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02211920.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02211920(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022119d8.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022119d8(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x022119e4.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_022119e4(void *scene, ...);\n\n/*\n * Process the recovered ABI inputs and offset-derived scene state for 0x02211a14.\n * Caller-owned, touch, graphics, GameWork, sound, or SDK state may change as\n * shown by the matching body; return-register and any MMIO behavior are preserved.\n * Exact field/callback roles remain inferred until stronger evidence is available.\n */\nextern "C" void *func_ov059_02211a14(void *scene, ...);\n\n
